import React from 'react';

interface IReportCardListProps {
  listing: any;
}

const ReportCardList: React.FC<IReportCardListProps> = ({ listing }): React.ReactElement => {
  return <div />;
};

export default ReportCardList;
