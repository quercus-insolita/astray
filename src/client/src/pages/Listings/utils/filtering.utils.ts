import { IReport } from '../../../models/report';
import { IListingFilters } from '../models/filters';

export const filterReportsListing = (listings: IReport[], filters: IListingFilters): IReport[] => {
  let result = listings;

  return result;
};
